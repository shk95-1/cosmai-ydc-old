# 어태커 세션 프롬프트

작업 단위가 끝나면 여기에 프롬프트를 갱신한다. **새 세션에 그대로 붙여 넣으면
그 세션이 이 작업을 공격한다.** 만든 세션이 자기 작업을 검토하면 같은 맹점을
두 번 보게 되므로, 문맥을 모르는 세션에 맡긴다.

`superpowers:requesting-code-review` 의 리뷰어 템플릿을 바탕으로, 이 프로젝트에서
실제로 세 번 겪은 실패 유형을 공격 목록에 넣었다.

---

## 현재 단위 — 검색·임베딩 준비 (`60de7fa`..`c54d1e7`)

아래를 새 세션에 붙여 넣으십시오.

```
당신은 이 리포를 처음 보는 적대적 검토자입니다. 목표는 칭찬이 아니라
**틀린 것을 찾는 것**입니다. 작업을 만든 세션의 문맥은 주지 않습니다 —
코드와 커밋만 보고 판단하십시오.

작업 디렉터리: C:\Users\Admin\Downloads\youtube-data-collector\youtube-data-collector
파이썬: C:/Users/Admin/miniconda3/python.exe
먼저 CLAUDE.md 를 읽으십시오. 정본·규칙·과거 실패가 적혀 있습니다.

## 검토 범위

git diff 60de7fa..c54d1e7

포함된 작업:
1. bm25.py — 청크 CSV 를 색인에 추가, 소스별 검색(by_source), 성분 사전 적용
2. commerce_chunks.py — 커머스 리뷰 18,476 청크 (신규)
3. encode_chunks.py — 벡터 인코딩 + 매니페스트 (신규, 모델 미설치 상태)
4. hybrid.py — RRF 순위 융합, 벡터 검색 (신규)
5. retrieval_eval.py — --engine {bm25,vector,hybrid} 추가

## 이 프로젝트가 실제로 세 번 당한 실패 — 같은 게 또 있는지 보십시오

(A) **수집·추출 방법이 관측값이 됐다**
    - 채널당 수집 상한 10 → 월 10건이 트렌드처럼 보였다
    - 정렬 없는 offset 페이징 → 중복 37%·깊이 변동이 "데이터 문제"로 보고됐다
    - 색인의 92%가 짧은 댓글 → 다른 소스가 검색에 안 나왔다
    셋 다 **오류가 안 나고 결과가 그럴듯했습니다.**

(B) **캐시 키에 설정을 빠뜨려 옛 결과를 계속 썼다** (두 번)
    bm25.build 의 캐시 스탬프에 지금 들어가는 요소가 충분한지 확인하십시오.
    토큰화·사전·청크·점수에 영향을 주는 것이 하나라도 빠지면 조용히 틀립니다.

(C) **오류 없이 조용히 나빠지는 설정**
    e5 프리픽스 누락, L2 정규화 불일치, dtype 불일치, 길이 상한 초과 시 뒤가 잘림.
    encode_chunks.py / hybrid.py 가 이걸 막고 있는지, 막았다고 착각만 하는지 보십시오.

## 반드시 실제로 실행해서 확인할 것

python bm25.py --demo
python commerce_chunks.py --demo
python encode_chunks.py --demo
python hybrid.py --demo
python retrieval_eval.py --demo
python chunks.py --validate reports/chunks_commerce.csv
python bm25.py --query "판테놀 쓰는 선크림" --per-source --top 2
python retrieval_eval.py --engine bm25 --mode heldout

(첫 BM25 색인은 4~5분 걸립니다. 캐시가 있으면 4초입니다.)

## 특히 의심할 것

1. **by_source 가 정말 소스별 상위 k 인가.** index.search(query, None) 으로
   점수 붙은 문서를 전부 받는데, 흔한 토큰 질의에서 몇 개나 정렬되는지,
   메모리·시간이 터지지 않는지 확인하십시오. 상한이 없습니다.

2. **retrieval_eval 의 --engine vector 가 공정한 비교인가.** heldout 의 정답을
   "질의 토큰이 든 문서 빼기"로 정의하는데, 이 정의는 BM25 토큰화에 의존합니다.
   벡터 검색기를 이 정답으로 채점하는 것이 정당한지 따져 보십시오.
   부당하다면 어떻게 재야 하는지 제시하십시오.

3. **hybrid 의 depth.** fused() 가 각 검색기에서 max(k*10, 100) 만 받아 합칩니다.
   한쪽에서 200위 밖에 있는 문서는 절대 못 올라옵니다. 이게 RRF 의 목적과
   맞는지, k=None 일 때 depth=200 이 적절한지 보십시오.

4. **commerce_chunks 의 doc_id 중복 제거.** 같은 리뷰가 여러 스냅샷에 잡히면
   먼저 온 것만 남깁니다(19,807 → 18,235). 정렬이 captured_at 오름차순이므로
   **가장 오래된 판**이 남습니다. 리뷰 본문이 수정되는 경우 최신본을 놓칩니다.
   이게 의도인지, 문제인지 판단하십시오.

5. **encode_chunks 의 리비전.** huggingface_hub 가 없으면 "unknown" 으로 적고
   경고만 하고 진행합니다. CLAUDE.md 는 리비전을 필수로 못 박았는데, 경고만
   하고 넘어가는 게 규칙과 맞는지 보십시오.

6. **성분 사전 정제 기준.** seeds/ingredient_dictionary.tsv 에서 21항목을 걸렀고
   별표 50개를 뗐습니다. 정상 성분명을 잘못 버렸을 가능성을 확인하십시오.
   (이 프로젝트에서 "길이만으로 스캔했다가 정상 INCI명을 손상으로 오판"한
   전례가 있습니다.)

7. **숫자가 문서와 코드에서 일치하는가.** CLAUDE.md §5 의 값들이 지금 코드로
   재생성되는지 확인하십시오. 특히 커머스 리뷰 청크 18,476 과 백탁 552.

## 출력 형식

### 강점
[구체적으로. 파일:줄]

### 문제

#### 치명 (반드시 고쳐야)
[조용히 틀린 답을 내는 것, 데이터 손실, 재현 불가]

#### 중요 (고쳐야)
[설계 결함, 검증 누락, 잘못된 비교]

#### 사소
[정리·문서]

문제마다: 파일:줄 · 무엇이 틀렸나 · 왜 문제인가 · 어떻게 고치나

### 확인하지 못한 것
[실행 못 한 것, 판단 못 한 것을 숨기지 말고 적으십시오]

### 판정
**이대로 써도 되나?** [예 | 아니오 | 고치면]
**근거:** [기술적으로 1~2문장]

## 규칙

- 읽기 전용입니다. 워킹 트리·인덱스·HEAD 를 바꾸지 마십시오
- 서브에이전트를 띄우지 마십시오. 직접 보십시오
- 실제로 읽지 않은 코드에 대해 말하지 마십시오
- "괜찮아 보인다" 로 끝내지 마십시오. 못 찾았으면 못 찾았다고 쓰십시오
- 사소한 것을 치명으로 올리지 마십시오
```

---

## 이전 단위 기록

| 단위 | 범위 | 결과 |
|---|---|---|
| 검색·임베딩 준비 | `60de7fa`..`c54d1e7` | (검토 대기) |
