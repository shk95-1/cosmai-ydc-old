# 어태커 세션 프롬프트

작업 단위가 끝나면 여기에 프롬프트를 갱신한다. **새 세션에 그대로 붙여 넣으면
그 세션이 이 작업을 공격한다.** 만든 세션이 자기 작업을 검토하면 같은 맹점을
두 번 보게 되므로, 문맥을 모르는 세션에 맡긴다.

`superpowers:requesting-code-review` 의 리뷰어 템플릿을 바탕으로, 이 프로젝트에서
실제로 겪은 실패 유형을 공격 목록에 넣었다.

---

## 현재 단위 — 논문 축 중지·성분 카드·시각화 (`73dca27`..`03b7f58`)

발표 전 마지막 단위다. **여기서 틀린 게 나오면 발표 자료가 틀린다.**

아래를 새 세션에 붙여 넣으십시오.

```
당신은 이 리포를 처음 보는 적대적 검토자입니다. 목표는 칭찬이 아니라
틀린 것을 찾는 것입니다. 작업을 만든 세션의 문맥은 주지 않습니다.

작업 디렉터리는 youtube-data-collector 리포입니다.
파이썬은 C:/Users/Admin/miniconda3/python.exe 를 쓰십시오.
PATH 의 python 은 Microsoft Store 스텁이라 "Python " 만 찍고 끝납니다.
먼저 CLAUDE.md 를 읽으십시오. 정본·규칙·과거 실패 다섯 건이 적혀 있습니다.

## 검토 범위

git diff 73dca27..03b7f58

**이 단위 안에서 결론이 한 번 뒤집혔습니다.** 08.25 오전에 논문 축을 붙였다가
오후에 내렸습니다. 그래서 **뒤집기 전 주장이 어딘가 남아 있는지**가 첫 번째
검사 대상입니다.

지금 주장하는 것은 다섯 개입니다. 전부 의심하십시오.

  1. 논문 축은 못 쓴다 — 잔존율 20~48% 이고 기준선 `cosmetic` 만 100.1% 라
     분모와 분자의 모집단이 다르다
  2. 그래도 결론 세 개 중 둘은 그대로다 — ①②③ 중 논문에 기댄 것은 없었다
  3. 수호님 2차 청크로 벡터가 0.042 -> 0.038 내려간 원인은 mfds 접두어다
     (heldout 상위10에서 mfds 칸이 23 -> 28)
  4. 카드 6장의 유형은 규칙이 배정했고 문턱을 결과에 맞추지 않았다
  5. `--chunks` 가 기본값에 덧붙던 것을 고쳤고 회귀가 없다 (literal P@10 0.841)

## 이 프로젝트가 다섯 번 당한 실패 — 여섯 번째가 여기 있는지 보십시오

전부 **오류가 안 나고 결과가 그럴듯했습니다.**

  수집 상한 10 이 관측값이 됐다
  정렬 없는 페이징이 "중복 37%" 라는 없는 결과를 만들었다
  색인 92%가 짧은 댓글이라 전역 상위 k 가 무의미했다
  정답은 doc_id 인데 검색 결과가 chunk_id 라 점수가 늘 0 이었다
  남의 벡터가 정규화 전 본문으로 인코딩돼 코사인 0.9986 이었다

패턴은 같습니다: **파이프라인이 조용히 답을 만들었다.**

## 반드시 실제로 실행할 것

  python -X utf8 -m unittest discover -s tests
  python -X utf8 cross_source.py
  python -X utf8 ingredient_cards.py --demo
  python -X utf8 ingredient_cards.py
  python -X utf8 cards.py
  python -X utf8 chunks.py --validate reports/chunks_ingredient_mfds.csv
  python -X utf8 reproduce.py data/panel/run_20260819T053057Z data/panel/run_20260819T054559Z

돌려 보지 않은 것에 대해 말하지 마십시오.

## 특히 의심할 것

1. `cosmetic` 을 기준선으로 쓴 것이 정당한가
   cross_source.py 의 paper_growth() 가 모든 성분 배수를 `cosmetic` 배수로 나눕니다.
   **이게 순환 논리가 아닌지 보십시오** — `cosmetic` 자체가 특정 성분들의 논문을
   포함하고 있다면 분모에 분자가 들어간 것입니다. 데이터로 확인할 수 있는지,
   확인할 수 없다면 그 한계가 문서에 적혀 있는지 판단하십시오.
   "차이 평균 0.083" 이 정말 일치의 증거인지, 아니면 같은 값으로 나눠서 생긴
   인공물인지 따지십시오.

2. `ratio_usable` 을 두 색인 AND 로 묶은 것
   cross_source.py 는 EPMC·PubMed 둘 다 usable 이어야 usable 로 봅니다.
   ingredient_cards.py 도 같습니다. 한쪽만 표본이 충분한 성분이 그래서 버려지는지,
   버려지는 게 맞는지 보십시오.

3. 유형 문턱을 결과에 맞춰 정하지 않았는가
   ingredient_cards.py 의 SEARCH_SURGE=20 · PRESCRIBE_ABSENT=1.0 ·
   PRESCRIBE_WIDE=30 · HIGH_DOSE_SHALLOW=5 · PAPER_AHEAD=1.0 · PAPER_BEHIND=0.9
   각 문턱을 조금 움직여 보고 **카드 구성이 뒤집히는지** 확인하십시오.
   뒤집힌다면 그 카드는 데이터가 아니라 문턱이 만든 것입니다.
   커밋 메시지는 "유행 반증 규칙에서 처방 조건을 뗐다" 고 주장합니다.
   그게 원칙에 따른 것인지 레티날을 넣으려던 것인지 판단하십시오.

4. `INGREDIENT_KEYS` 의 부분문자열 매칭
   "시카센텔라" 는 ("센텔라","시카") 로 찾습니다. **"시카" 가 다른 성분명에
   부분문자열로 들어가는 경우가 없는지** 성분표 전수로 확인하십시오.
   "레티날" 은 ("레티날","레티놀") 로 둘을 합쳐 셉니다. 논문 검색어는
   retinaldehyde 하나입니다. 이 불일치가 결론을 바꾸는지 보십시오.
   cross_source.py 와 ingredient_cards.py 의 KEYS 가 서로 다르면 그 자체가 버그입니다.

5. 배합순위 중앙값의 정의가 두 곳에서 다르다
   cross_source.py 는 성분 등장 전체의 중앙(46위)을 냅니다.
   ingredient_cards.py 의 products_with() 는 제품별 최선 순번을 씁니다(31위).
   카드가 이 차이를 한계에 적어 뒀습니다. **적어 두는 것으로 충분한지,
   아니면 한쪽이 틀린 것인지** 판단하십시오.

6. 고함량 정의
   high_dose_pct 는 "배합순위 10위 이내 비율" 입니다. 배합 순위는 함량 순이지만
   **순위가 함량을 보장하지 않습니다** (10위가 0.1% 일 수 있습니다).
   "고함량" 이라는 말이 이 정의를 과대 표현하는지 판단하십시오.
   ①③ 결론(널리 얕게 / 널리 깊게)이 전부 이 정의 위에 서 있습니다.

7. 벡터 하락 원인 규명이 성립하는가
   "mfds 가 heldout 상위10에서 23 -> 28 칸으로 늘었다" 가 0.042 -> 0.038 과
   Hit 17% -> 13% 를 실제로 설명하는지 산수로 확인하십시오.
   5칸이 P@10 4/1000 과 질의 2개를 설명할 수 있는 크기인지 따지십시오.
   설명이 안 되면 **다른 원인이 있고 그걸 놓친 것**입니다.
   .cache/vectors/e5base 와 scratchpad 의 수호님 1차 폴더로 옛 벡터를 되살려
   직접 재현할 수 있습니다.

8. 담론 언급 수
   PDRN 카드가 "유튜브 1,522" 를 근거로 냅니다. 커밋 메시지는 그중 선크림 단어가
   같이 있는 것이 187건이라고 합니다. **1,522 를 근거로 실은 것이 정당한지**
   판단하십시오. 한계에 적었다고 해서 근거가 되는지 따지십시오.

9. 정정된 성분 수가 맞는가
   나이아신아마이드 400제품 / 아데노신 402제품 을 성분표에서 직접 세어 보십시오.
   577제품 분모가 맞는지도 확인하십시오.
   문서·CLAUDE.md·카드·대조표에 같은 값이 들어가 있는지 대조하십시오.

10. **뒤집기 전 숫자가 남아 있는가** — 이 단위에서 가장 위험한 자리입니다
    `grep -rn "1.17\|1.33\|0.53\|0.46\|세 축\|선행 연구 기회\|유행 반증" reports/ *.md`
    논문 배수·엑소좀 "세 축"·레티날 "논문이 반증" 이 살아 있는 문서가 있는지
    보십시오. `PAPER_HOLD = True` 인데 어딘가 옛 표가 남아 있으면 발표에서 갈립니다.
    `reports/` 의 ASCII 사본(pipeline_flow · youtube_part_status · progress_summary)이
    한글 원본과 같은 내용인지도 대조하십시오.

11. **시각 대시보드가 산출물과 같은 숫자를 내는가** — `viz_dashboard.py`
    "산출물에서 그대로 읽는다" 고 주장합니다. `collect()` 가 CSV 값을 바꾸지 않는지,
    상수로 박아 둔 값(`CHUNK_SOURCES`·`RETENTION`·`RETRIEVAL`·`BACKTEST`)이
    실제 산출물과 맞는지 대조하십시오. **상수로 박은 것 자체가 위험**입니다 —
    산출물이 바뀌어도 안 따라갑니다.
    커머스 랭킹은 `commerce_ranking.csv`(상위 20위)와 `_raw`(100위권)가 다릅니다.
    어느 쪽을 썼는지, 문서의 380/38 과 왜 다른지 판단하십시오.

12. 500자 초과 27건
    chunks.py 의 검증기가 [통과] 를 내면서 [주의] 로 27건을 알립니다.
    이 설계가 맞는지, 아니면 500자가 계약이면 위반으로 세워야 하는지 판단하십시오.
    e5 상한 512토큰에 562자가 실제로 잘리는지 계산하십시오.

## 출력 형식

### 강점
구체적으로. 파일:줄

### 문제
#### 치명 (반드시 고쳐야)
#### 중요 (고쳐야)
#### 사소

문제마다: 파일:줄 · 무엇이 틀렸나 · 왜 문제인가 · 어떻게 고치나

### 다섯 주장에 대한 판정
각각: 지지 / 반박 / 판단 불가 + 근거

### 뒤집기 전 숫자가 남아 있는 파일
파일:줄 로 적으십시오. 없으면 없다고 쓰십시오

### 발표에 쓰면 안 되는 숫자
있으면 적으십시오. 없으면 없다고 쓰십시오

### 확인하지 못한 것
숨기지 말고 적으십시오

### 판정
이대로 발표에 써도 되나? 예 / 아니오 / 고치면
근거: 1~2문장

## 규칙

- 읽기 전용입니다. 워킹 트리·인덱스·HEAD 를 바꾸지 마십시오
- 서브에이전트를 띄우지 마십시오. 직접 보십시오
- 실제로 읽지 않은 코드에 대해 말하지 마십시오
- "괜찮아 보인다" 로 끝내지 마십시오. 못 찾았으면 못 찾았다고 쓰십시오
- 사소한 것을 치명으로 올리지 마십시오
- 문턱을 움직여 보라는 항목은 **실제로 움직여서** 결과를 붙이십시오
```

---

## 이전 단위 기록

| 단위 | 범위 | 결과 |
|---|---|---|
| 검색·임베딩 준비 | `60de7fa`..`c54d1e7` | 검토 안 함 — 다음 단위에 흡수 |
| 벡터 임베딩·검색기 비교 | `c54d1e7`..`73dca27` | 검토 안 함 — 자체 확인 3건으로 대체 |
| 논문 축 중지·성분 카드·시각화 | `73dca27`..`03b7f58` | **검토 대기 — 발표 전 마지막** |

### 자체 확인으로 대체한 것 (`c54d1e7`..`73dca27`)

어태커 세션을 못 돌려서 의심 7개 중 3개를 직접 확인했다.

```
성분 사전이 literal 을 0.862 -> 0.828 로 깎은 이유    성분명·주제 별칭 겹침 4개
벡터가 이긴 자리                                    공백 든 별칭 (톤 업 · 눈 시림)
캐시 키 누락                                        USER_WORD_SCORE · 사전 해시 2건
```

남은 4개는 이번 단위 프롬프트에 흡수했다.
