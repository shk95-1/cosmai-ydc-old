# 어태커 세션 프롬프트

작업 단위가 끝나면 여기에 프롬프트를 갱신한다. **새 세션에 그대로 붙여 넣으면
그 세션이 이 작업을 공격한다.** 만든 세션이 자기 작업을 검토하면 같은 맹점을
두 번 보게 되므로, 문맥을 모르는 세션에 맡긴다.

`superpowers:requesting-code-review` 의 리뷰어 템플릿을 바탕으로, 이 프로젝트에서
실제로 겪은 실패 유형을 공격 목록에 넣었다.

---

## 현재 단위 — 벡터 임베딩과 검색기 비교 (`c54d1e7`..`73dca27`)

아래를 새 세션에 붙여 넣으십시오.

```
당신은 이 리포를 처음 보는 적대적 검토자입니다. 목표는 칭찬이 아니라
틀린 것을 찾는 것입니다. 작업을 만든 세션의 문맥은 주지 않습니다.

작업 디렉터리는 youtube-data-collector 리포입니다.
파이썬은 C:/Users/Admin/miniconda3/python.exe 를 쓰십시오.
PATH 의 python 은 Microsoft Store 스텁이라 "Python " 만 찍고 끝납니다.
먼저 CLAUDE.md 를 읽으십시오. 정본·규칙·과거 실패 네 건이 적혀 있습니다.

## 검토 범위

git diff c54d1e7..73dca27

이 단위가 주장하는 결론은 두 개입니다. 둘 다 의심하십시오.

  주장 1: 임베딩이 채택 기준을 넘었다 (heldout P@10 0.000 -> 0.042, Hit 17%)
  주장 2: 하이브리드(RRF)는 쓰면 안 된다 (양쪽에서 중간)

## 이 프로젝트가 오늘 네 번 당한 실패 — 다섯 번째가 여기 있는지 보십시오

  (1) 수집 상한 10 이 관측값이 됐다
  (2) 정렬 없는 offset 페이징이 "중복 37%" 를 만들었다
  (3) 색인의 92%가 짧은 댓글이라 다른 소스가 검색에 안 나왔다
  (4) 정답은 doc_id 인데 검색 결과가 chunk_id 여서 벡터 점수가 항상 0 이었다

넷 다 오류가 안 나고 결과가 그럴듯했습니다. (4)는 이 단위에서 고친 것인데,
고친 방식(to_docs)이 또 다른 편향을 만들지 않았는지 보십시오.

## 반드시 실제로 실행할 것

  PY=C:/Users/Admin/miniconda3/python.exe

  $PY -X utf8 retrieval_eval.py --demo
  $PY -X utf8 hybrid.py --demo
  $PY -X utf8 encode_chunks.py --demo

  for e in bm25 vector hybrid; do
    for m in literal heldout; do
      $PY -X utf8 retrieval_eval.py --engine $e --mode $m
    done
  done

벡터는 .cache/vectors/e5base.npy 에 있습니다 (913MB, 297,392 x 768).
BM25 색인 캐시가 없으면 첫 빌드에 4~5분 걸립니다.

## 특히 의심할 것

1. to_docs 가 검색기마다 불공정하지 않은가 (retrieval_eval.py)
   BM25 는 document.csv 색인이라 이미 doc_id 를 돌려주므로 to_docs 가 거의
   무동작이고, 벡터는 chunk_id 를 돌려주므로 중복 제거가 일어납니다. 즉
   벡터만 K*5 를 받아 K개 문서로 줄입니다. 이게 벡터에 유리한지 불리한지
   계산해 보십시오. BM25 도 청크 색인 부분(성분·식약처)에서는 chunk_id 를
   돌려주므로 섞여 있습니다.

2. heldout 정답 정의가 벡터에 공정한가
   정답을 "질의 토큰이 든 문서 빼기" 로 정하는데, 이 정의는 BM25 토큰화에
   의존합니다. 벡터는 그 토큰화를 모릅니다. docs_with_tokens 가 빼는 문서
   집합이 벡터에게도 타당한 제외인지 따지십시오. 부당하다면 어떻게 재야
   하는지 제시하십시오.

3. 0.042 가 잡음인지 신호인지
   질의 60개 중 Hit 10개입니다. 이 표본으로 "채택 기준 통과" 를 주장할 수
   있습니까? reports/retrieval_eval_heldout_vector.csv 에서 어느 주제가
   맞혔는지 보고, 한두 주제에 몰려 있으면 그 사실을 지적하십시오.

4. 하이브리드가 나쁘다는 결론이 RRF 설정 탓은 아닌가
   hybrid.py 의 fused() 가 각 검색기에서 max(k*10, 100) 만 받습니다.
   RRF_K=60 도 관행값이고 이 데이터로 뽑지 않았습니다. depth 나 k 를 바꾸면
   결론이 뒤집히는지 실제로 돌려 보십시오. 뒤집히면 "하이브리드는 나쁘다" 가
   아니라 "우리 RRF 설정이 나쁘다" 입니다.

5. BM25 literal 이 0.862 -> 0.828 로 떨어졌다
   이 단위 전에는 0.862 였습니다. to_docs 때문인지, 청크 색인이 늘어서인지,
   다른 이유인지 찾으십시오. 문서에 이 변화가 설명돼 있지 않습니다.

6. 매니페스트 대조가 없다
   encode_chunks 가 모델·리비전·프리픽스·L2·chunk_files 해시를 매니페스트에
   적지만, hybrid.Vectors 는 l2_normalized 만 확인합니다. 청크 파일이 바뀌어도
   옛 벡터를 그대로 씁니다. CLAUDE.md 는 "캐시 키에 설정을 빠뜨려 옛 결과를
   쓴 실수를 두 번 했다" 고 적어 뒀습니다. 같은 실수인지 판단하십시오.

7. 비교가 재현되는가
   위 6개 조합을 다시 돌려 CLAUDE.md 의 표와 일치하는지 확인하십시오.
   벡터 검색은 GPU 없이 numpy 로도 돌아야 합니다.

## 출력 형식

### 강점
구체적으로. 파일:줄

### 문제
#### 치명 (반드시 고쳐야)
#### 중요 (고쳐야)
#### 사소

문제마다: 파일:줄 · 무엇이 틀렸나 · 왜 문제인가 · 어떻게 고치나

### 두 주장에 대한 판정
주장 1 (임베딩 채택): 지지 / 반박 / 판단 불가 + 근거
주장 2 (하이브리드 폐기): 지지 / 반박 / 판단 불가 + 근거

### 확인하지 못한 것
숨기지 말고 적으십시오

### 판정
이대로 발표에 써도 되나? 예 / 아니오 / 고치면
근거: 1~2문장

## 규칙

- 읽기 전용입니다. 워킹 트리·인덱스·HEAD 를 바꾸지 마십시오
- 서브에이전트를 띄우지 마십시오. 직접 보십시오
- 실제로 읽지 않은 코드에 대해 말하지 마십시오
- "괜찮아 보인다" 로 끝내지 마십시오. 못 찾았으면 못 찾았다고 쓰십시오
- 사소한 것을 치명으로 올리지 마십시오
```

---

## 이전 단위 기록

| 단위 | 범위 | 결과 |
|---|---|---|
| 검색·임베딩 준비 | `60de7fa`..`c54d1e7` | 검토 안 함 — 다음 단위에 흡수 |
| 벡터 임베딩·검색기 비교 | `c54d1e7`..`73dca27` | 검토 대기 |
