.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.env.example
data/*
_archive/
.env
.venv/
__pycache__/
*.pyc

# 댓글 원문이 그대로 들어 있어 올리지 않는다. 기획안 §4 — 원문은 분석 목적
# 범위에서만 보관하고 재배포하지 않는다. 생성은 evidence_comments.py 로 재현한다.
reports/evidence_comments.csv
common/*.csv
common/*.zip

# 댓글 원문을 그대로 담으므로 evidence_comments.csv 와 같은 규칙을 적용한다.
# 생성은 dashboard.py 로 재현한다.
reports/dashboard.html

# 댓글 원문이 실려 있어 evidence_comments.csv 와 같은 규칙을 적용한다. cards.py 로 재현한다.
reports/opportunity_cards.md
reports/opportunity_cards.json

# 정규화본은 수호님 원본에서 재생성한다. normalize_ingredients.py 로 재현.
reports/ingredient_normalized.csv
reports/unmatched_terms.csv
reports/spam_ad_sensitivity.csv
reports/backtest.csv
reports/reproducibility.json

# 수집 run 은 올리지 않지만(용량·원문), 팀원이 파일로 준 원천은 리포에 둔다.
# 카톡 폴더를 정본으로 두면 파일이 사라지는 순간 재현이 끊긴다 — 실제로 한 번 끊겼다.
!data/external/
.cache/
reports/retrieval_eval_*.csv
